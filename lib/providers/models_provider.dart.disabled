import 'package:flutter_data/flutter_data.dart';

import '../models/component_business_model.dart';

class ModelNotifier extends StateNotifier<List<Model>> {
  ModelNotifier(super.state);

  void setModels(List<Model> models) => state = models;
}

final modelsProvider =
    StateNotifierProvider<ModelNotifier, List<Model>>((_) => ModelNotifier([]));
